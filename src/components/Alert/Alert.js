import React from 'react';

const Alert = () => {
    return (
        <div className="alert alert-danger" role="alert">
            It is already added in the team
        </div>
    );
};

export default Alert;